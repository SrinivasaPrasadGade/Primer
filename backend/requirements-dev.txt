# Test-only dependencies. Kept out of requirements.txt so they don't ship in the
# API image.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#     pytest
#
# pytest-asyncio must be pinned alongside pytest: the whole suite is async, and
# an incompatible pair fails at *collection* rather than in a test, so every
# module errors out and nothing runs at all. pytest-asyncio 0.23 against
# pytest 9 raises "AttributeError: 'Package' object has no attribute 'obj'"
# before a single test executes. 1.4.0 declares pytest>=8.4,<10.
pytest==9.0.2
pytest-asyncio==1.4.0

# fastapi.testclient (used by tests/test_api.py) is a thin wrapper over httpx
# and raises at import without it.
httpx==0.27.0
